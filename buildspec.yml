version: 0.2
phases:
  install:
    commands:
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket pdomala-lambda-pipeline --output-template-file outputtemplate.yaml
artifacts:
  type: zip
  files:
    - samTemplate.yaml
    - outputtemplate.yaml
