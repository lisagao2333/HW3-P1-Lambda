version: 0.2
phases:
  install:
    commands:
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket codepipeline-us-east-1-336994830365 --output-template-file outputSamTemplate.yml
artifacts:
  type: zip
  files:
    - samTemplate.yaml
    - outputSamTemplate.yml
